# Codemancer adapters

1.18.0 binds the package to the generic `mancer/package-artifact-store/v1` and `mancer/schema-workbench-ui/v1` adapters. Repository mutation, command execution and deployment adapters are intentionally not granted in this release.
