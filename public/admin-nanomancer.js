const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let csrf='',capability=null,archiveObjects=[],runs=[],analysis=null,loading=false;
function workspace(){return window.anomancerWorkspaces?.current?.()||{id:'default',name:'Anomancer'};}
function headers(extra={}){return{'X-Anomancer-Workspace':workspace().id,...extra};}
async function session(){if(csrf)return csrf;const r=await fetch('/api/admin/auth?resource=session',{credentials:'same-origin'}),d=await r.json().catch(()=>({}));if(!r.ok||!d.authenticated)throw new Error('Admin-session puuttuu.');csrf=d.csrf||'';return csrf;}
function status(text='',kind=''){const el=q('#nanomancerStatus');if(!el)return;el.textContent=text;el.dataset.kind=kind;}
function valuePreview(v){if(v===null||v===undefined)return'—';if(typeof v==='string')return v.length>180?`${v.slice(0,180)}…`:v;try{const s=JSON.stringify(v);return s.length>180?`${s.slice(0,180)}…`:s;}catch{return String(v);}}
function renderContract(){const el=q('#nanomancerContract');if(!el)return;if(!capability){el.innerHTML='<span>Capability Registry ei ole käytettävissä.</span>';return;}el.innerHTML=[`plugin ${capability.version}`,capability.deterministic?'deterministinen':'',capability.permissions?.modelAccess==='none'?'ei mallia':'',capability.permissions?.workspaceWrite==='none'?'ei workspace-writeä':'',capability.permissions?.archiveWrite==='none'?'ei archive-writeä':'',`${capability.operations.length} operaatiota`].filter(Boolean).map(x=>`<span>${esc(x)}</span>`).join('');}
function options(kind){if(kind==='archive')return archiveObjects.map(x=>({id:x.id,label:`${x.title} · ${x.type} · ${x.workspaceId}`}));if(kind==='run')return runs.map(x=>({id:x.id,label:`${x.orchestra?.name||x.orchestra?.id||'Ajo'} · ${x.id} · ${x.status}`}));return[];}
function renderSource(fieldset){const kind=fieldset.querySelector('[data-nano-kind]').value,refWrap=fieldset.querySelector('[data-nano-ref-wrap]'),inlineWrap=fieldset.querySelector('[data-nano-inline-wrap]'),select=fieldset.querySelector('[data-nano-ref]');refWrap.hidden=kind==='inline';inlineWrap.hidden=kind!=='inline';if(kind!=='inline'){const rows=options(kind);select.innerHTML=rows.length?rows.map(x=>`<option value="${esc(x.id)}">${esc(x.label)}</option>`).join(''):'<option value="">Ei kohteita</option>';}}
function renderSources(){qa('[data-nano-source]').forEach(renderSource);}
async function loadRegistryAndData(){if(loading)return;loading=true;status('Nanomancerin rekisteriä ladataan…');try{const wid=workspace().id;const [c,a,r]=await Promise.all([fetch('/api/admin/core?resource=capabilities',{credentials:'same-origin',headers:headers()}),fetch('/api/admin/core?resource=archive&limit=250',{credentials:'same-origin'}),fetch('/api/admin/core?resource=runs&limit=60',{credentials:'same-origin',headers:headers()})]);const cd=await c.json().catch(()=>({})),ad=await a.json().catch(()=>({})),rd=await r.json().catch(()=>({}));if(!c.ok||!cd.ok)throw new Error(cd.message||'Capability Registry ei auennut.');capability=(cd.capabilities||[]).find(x=>x.id==='nanomancer')||null;archiveObjects=a.ok&&ad.ok?ad.objects||[]:[];runs=r.ok&&rd.ok?rd.runs||[]:[];renderContract();renderSources();status(`${workspace().name||wid} · ${archiveObjects.length} arkisto-objektia · ${runs.length} ajoa`,'ok');}catch(error){status(error.message,'error');}finally{loading=false;}}
function sourceInput(fieldset,index){const kind=fieldset.querySelector('[data-nano-kind]').value;if(kind==='inline'){const text=fieldset.querySelector('[data-nano-inline]').value.trim();let value;try{value=JSON.parse(text);}catch{throw new Error(`${index?'B':'A'}: JSON ei ole kelvollinen.`);}return{kind:'inline',id:`inline-${index+1}`,label:`Inline ${index?'B':'A'}`,value};}const id=fieldset.querySelector('[data-nano-ref]').value;if(!id)throw new Error(`${index?'B':'A'}: valitse vertailukohde.`);return{kind,id};}
function renderResult(){const host=q('#nanomancerResult'),save=q('#nanomancerArchiveResult');if(!host)return;if(!analysis){save.disabled=true;return;}save.disabled=false;const s=analysis.summary||{};host.innerHTML=`<div class="nanomancer-result-meta"><span>${esc(analysis.operation)}</span><span>${esc(analysis.plugin?.id)} ${esc(analysis.plugin?.version)}</span><span>modelUsed: ${analysis.plugin?.modelUsed?'true':'false'}</span><code>${esc(analysis.integrity?.analysisHash||'')}</code></div><div class="nanomancer-metrics"><article><span>Syötteet</span><strong>${s.inputCount||0}</strong></article><article><span>Vertailut</span><strong>${s.comparisonCount||0}</strong></article><article><span>Muuttuneet</span><strong>${s.changedPaths||0}</strong></article><article><span>Lisätty / poistettu</span><strong>${(s.addedPaths||0)+(s.removedPaths||0)}</strong></article><article><span>Numeeriset</span><strong>${s.numericChanges||0}</strong></article></div><section class="nanomancer-findings">${(analysis.findings||[]).map(f=>`<article class="nanomancer-finding" data-severity="${esc(f.severity)}"><code>${esc(f.code)}</code><span>${esc(f.message)}</span></article>`).join('')}</section>${(analysis.comparisons||[]).map((cmp,i)=>`<details class="nanomancer-comparison" ${i===0?'open':''}><summary>${esc(cmp.leftId)} ↔ ${esc(cmp.rightId)} · ${(cmp.changedPaths||0)+(cmp.addedPaths||0)+(cmp.removedPaths||0)} eroa</summary><table class="nanomancer-change-table"><thead><tr><th>Polku</th><th>Tyyppi</th><th>Ennen</th><th>Jälkeen</th><th>Δ</th></tr></thead><tbody>${(cmp.changes||[]).map(ch=>`<tr><td><code>${esc(ch.path)}</code></td><td>${esc(ch.kind)}</td><td><code>${esc(valuePreview(ch.before))}</code></td><td><code>${esc(valuePreview(ch.after))}</code></td><td>${ch.numeric?`${Number(ch.numeric.delta).toFixed(4)}${ch.numeric.percent===null?'':` · ${Number(ch.numeric.percent).toFixed(2)} %`}`:'—'}</td></tr>`).join('')||'<tr><td colspan="5">Ei poikkeamia.</td></tr>'}</tbody></table></details>`).join('')}`;}
async function run(){if(loading)return;try{await session();const inputs=qa('[data-nano-source]').map(sourceInput),operation=q('#nanomancerOperation').value;if(operation==='cross-run'&&inputs.some(x=>x.kind!=='run'))throw new Error('Cross-run tarvitsee kaksi ajotietuetta.');loading=true;q('#nanomancerRun').disabled=true;status('Nanomancer vertaa rakenteita…');const r=await fetch('/api/admin/core?resource=capabilities',{method:'POST',credentials:'same-origin',headers:headers({'Content-Type':'application/json','X-CSRF-Token':csrf}),body:JSON.stringify({pluginId:'nanomancer',operation,inputs})}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok){if(r.status===403)csrf='';throw new Error(d.message||'Nanomancer-ajo epäonnistui.');}analysis=d.analysis;renderResult();status(`Valmis · ${analysis.summary?.changedPaths||0} muuttunutta polkua · ${analysis.contextReceiptId?'Context Receipt '+analysis.contextReceiptId:'ei Archive-kontekstia'}`,'ok');}catch(error){status(error.message,'error');}finally{loading=false;q('#nanomancerRun').disabled=false;}}
async function archiveResult(){if(!analysis)return;try{await session();status('Valmistellaan ihmisen hyväksymää arkistointia…');const listing=await fetch('/api/admin/core?resource=archive&limit=1',{credentials:'same-origin'}),ld=await listing.json().catch(()=>({}));if(!listing.ok||!ld.ok)throw new Error(ld.message||'Archive revision ei auennut.');const object={type:'report',title:`Nanomancer · ${analysis.operation} · ${new Date(analysis.generatedAt).toLocaleString('fi-FI')}`,summary:(analysis.findings||[]).slice(0,3).map(x=>x.message).join(' ').slice(0,1200),workspaceId:workspace().id,tags:['nanomancer','analysis',analysis.operation],content:{json:analysis,text:`Nanomancer ${analysis.operation}: ${(analysis.summary?.changedPaths||0)+(analysis.summary?.addedPaths||0)+(analysis.summary?.removedPaths||0)} poikkeamaa.`},provenance:{createdBy:'human-approved-nanomancer',sourceRunId:analysis.orchestraContext?.runId||'',agentId:'',model:''},source:{kind:'capability-plugin',id:'nanomancer',label:'Nanomancer 1.17.2'}};const r=await fetch('/api/admin/core?resource=archive',{method:'POST',credentials:'same-origin',headers:headers({'Content-Type':'application/json','X-CSRF-Token':csrf}),body:JSON.stringify({action:'put',humanApproved:true,expectedRevision:ld.revision,object})}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error(d.message||'Analyysin arkistointi epäonnistui.');status(`Analyysi tallennettiin Arkistoon: ${d.object.id}`,'ok');window.anomancerArchive?.load?.();}catch(error){status(error.message,'error');}}
q('#nanomancerOperation')?.addEventListener('change',event=>{if(event.target.value==='cross-run'){qa('[data-nano-kind]').forEach(el=>el.value='run');renderSources();}});qa('[data-nano-kind]').forEach(el=>el.addEventListener('change',()=>renderSource(el.closest('[data-nano-source]'))));q('#nanomancerRun')?.addEventListener('click',run);q('#nanomancerArchiveResult')?.addEventListener('click',archiveResult);window.addEventListener('anomancer:workspace-ready',loadRegistryAndData);window.addEventListener('anomancer:workspace-change',()=>{analysis=null;renderResult();loadRegistryAndData();});window.addEventListener('anomancer:admin-ready',loadRegistryAndData);window.anomancerNanomancer={load:loadRegistryAndData,run,getAnalysis:()=>analysis};
