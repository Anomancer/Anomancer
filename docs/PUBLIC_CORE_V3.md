# Public Core v3

Public Core v3 derives its capability and execution architecture from the same declarative registries used by the runtime.

Flow:

capabilities/* → Capability Package Registry → public architecture allowlist → core-public.json → /core and /en/core

The public view exposes architecture, safe routing and availability metadata. It does not expose raw package permissions/contracts, provider configuration, prompts, workspace data or real run contents.
