import { CORE_VERSION } from './core-registry.js';
import { DEFAULT_WORKSPACE_ID, workspaceStorageScope } from './workspace-store.js';
import { NARRAMANCER_ARTIFACT_STATE_FORMAT, emptyNarramancerProject, normalizeNarramancerProject } from './narramancer-project.js';

const DEFAULT_TAG='anomancer-private-artifact';
const DEFAULT_PATH='.anomancer/private-workspace-artifact.json';
const CACHE_TTL_MS=3000;
const clone=v=>JSON.parse(JSON.stringify(v));
const clean=v=>String(v??'').trim();
const caches=new Map(),memoryStates=new Map();
function workspaceKey(id){return clean(id)||DEFAULT_WORKSPACE_ID;}
function mode(){const forced=clean(process.env.ANOMANCER_ARTIFACT_STORE).toLowerCase();if(forced==='memory')return'memory';if(forced==='github-tag')return'github-tag';if(process.env.GITHUB_CONTENT_TOKEN&&process.env.GITHUB_REPO?.includes('/'))return'github-tag';return'unconfigured';}
function cfg(workspaceId){const token=process.env.GITHUB_CONTENT_TOKEN,repo=process.env.GITHUB_REPO,branch=process.env.GITHUB_BRANCH||'master';if(!token||!repo?.includes('/'))throw Object.assign(new Error('Private Artifact Store ei ole konfiguroitu.'),{statusCode:503,code:'ARTIFACT_STORE_UNCONFIGURED'});const scope=workspaceStorageScope(workspaceId,{baseTag:clean(process.env.ANOMANCER_ARTIFACT_TAG)||DEFAULT_TAG,basePath:clean(process.env.ANOMANCER_ARTIFACT_PATH)||DEFAULT_PATH});return{token,repo,branch,...scope};}
export function privateArtifactStoreStatus(workspaceId){const id=workspaceKey(workspaceId),m=mode(),base={format:NARRAMANCER_ARTIFACT_STATE_FORMAT,coreVersion:CORE_VERSION,workspaceId:id,mode:m,durable:m==='github-tag',serverAuthoritative:true,publicBranchWrite:false,secretsExposed:false};if(m==='github-tag'){const c=cfg(id);return{...base,configured:true,ref:`refs/tags/${c.tag}`,path:c.path,legacyDefault:c.legacy};}return{...base,configured:m==='memory'};}
function defaultState(workspaceId){const id=workspaceKey(workspaceId);return{format:NARRAMANCER_ARTIFACT_STATE_FORMAT,coreVersion:CORE_VERSION,workspaceId:id,revision:0,updatedAt:'',project:emptyNarramancerProject(id)};}
function normalizeState(raw={},workspaceId){const id=workspaceKey(workspaceId);return{format:NARRAMANCER_ARTIFACT_STATE_FORMAT,coreVersion:CORE_VERSION,workspaceId:id,revision:Math.max(0,Number(raw.revision)||0),updatedAt:clean(raw.updatedAt),project:normalizeNarramancerProject(raw.project||{},id)};}
async function gh(workspaceId,path,options={}){const{token}=cfg(workspaceId);const response=await fetch(`https://api.github.com${path}`,{...options,headers:{Accept:'application/vnd.github+json',Authorization:`Bearer ${token}`,'X-GitHub-Api-Version':'2022-11-28','User-Agent':'anomancer-private-artifacts-v16.7',...(options.headers||{})}});const text=await response.text();let data=null;try{data=text?JSON.parse(text):null;}catch{data={message:text};}if(!response.ok)throw Object.assign(new Error(data?.message||`GitHub ${response.status}`),{statusCode:response.status===404?404:(response.status===409||response.status===422?409:502),code:`ARTIFACT_GITHUB_${response.status}`,githubStatus:response.status,details:data});return data;}
async function tagRef(workspaceId){const{repo,tag}=cfg(workspaceId);try{return clean((await gh(workspaceId,`/repos/${repo}/git/ref/tags/${encodeURIComponent(tag)}`))?.object?.sha)||null;}catch(e){if(e.statusCode===404)return null;throw e;}}
async function readTag(workspaceId){const{repo,tag,path}=cfg(workspaceId);try{const d=await gh(workspaceId,`/repos/${repo}/contents/${encodeURI(path)}?ref=${encodeURIComponent(tag)}`);return normalizeState(JSON.parse(Buffer.from(String(d?.content||'').replace(/\n/g,''),'base64').toString('utf8')),workspaceId);}catch(e){if(e.statusCode===404)return defaultState(workspaceId);throw e;}}
async function branchHead(workspaceId){const{repo,branch}=cfg(workspaceId);return clean((await gh(workspaceId,`/repos/${repo}/git/ref/heads/${encodeURI(branch)}`))?.object?.sha);}
async function commitTree(workspaceId,sha){const{repo}=cfg(workspaceId);return clean((await gh(workspaceId,`/repos/${repo}/git/commits/${sha}`))?.tree?.sha);}
async function write(workspaceId,state,{parentSha,create=false}={}){const{repo,tag,path}=cfg(workspaceId),base=await commitTree(workspaceId,parentSha);const blob=await gh(workspaceId,`/repos/${repo}/git/blobs`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content:`${JSON.stringify(state,null,2)}\n`,encoding:'utf-8'})});const tree=await gh(workspaceId,`/repos/${repo}/git/trees`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({base_tree:base,tree:[{path,mode:'100644',type:'blob',sha:blob.sha}]})});const commit=await gh(workspaceId,`/repos/${repo}/git/commits`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:`private artifact (${workspaceKey(workspaceId)}): revision ${state.revision}`,tree:tree.sha,parents:[parentSha]})});if(create)await gh(workspaceId,`/repos/${repo}/git/refs`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ref:`refs/tags/${tag}`,sha:commit.sha})});else await gh(workspaceId,`/repos/${repo}/git/refs/tags/${encodeURIComponent(tag)}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({sha:commit.sha,force:false})});return commit.sha;}
export async function loadPrivateArtifact({force=false,workspaceId}={}){const id=workspaceKey(workspaceId),m=mode();if(m==='memory'){if(!memoryStates.has(id))memoryStates.set(id,defaultState(id));return clone(memoryStates.get(id));}if(m==='unconfigured')throw Object.assign(new Error('Private Artifact Store ei ole konfiguroitu.'),{statusCode:503,code:'ARTIFACT_STORE_UNCONFIGURED'});const cached=caches.get(id);if(!force&&cached&&cached.expiresAt>Date.now())return clone(cached.state);const ref=await tagRef(id),state=ref?await readTag(id):defaultState(id);caches.set(id,{state:normalizeState(state,id),refSha:ref,expiresAt:Date.now()+CACHE_TTL_MS});return clone(caches.get(id).state);}
export async function savePrivateArtifact(project,{expectedRevision=null,workspaceId}={}){const id=workspaceKey(workspaceId),m=mode();if(m==='memory'){const current=await loadPrivateArtifact({force:true,workspaceId:id});if(expectedRevision!==null&&Number(expectedRevision)!==current.revision)throw Object.assign(new Error('Narramancer-projekti muuttui toisessa istunnossa.'),{statusCode:409,code:'ARTIFACT_REVISION_CONFLICT'});const next=normalizeState({revision:current.revision+1,updatedAt:new Date().toISOString(),project},id);memoryStates.set(id,next);return clone(next);}if(m==='unconfigured')throw Object.assign(new Error('Private Artifact Store ei ole konfiguroitu.'),{statusCode:503,code:'ARTIFACT_STORE_UNCONFIGURED'});for(let attempt=0;attempt<3;attempt++){caches.delete(id);const current=await loadPrivateArtifact({force:true,workspaceId:id}),cached=caches.get(id),ref=cached?.refSha||await tagRef(id);if(expectedRevision!==null&&Number(expectedRevision)!==current.revision)throw Object.assign(new Error('Narramancer-projekti muuttui toisessa istunnossa.'),{statusCode:409,code:'ARTIFACT_REVISION_CONFLICT'});const next=normalizeState({revision:current.revision+1,updatedAt:new Date().toISOString(),project},id);try{await write(id,next,{parentSha:ref||await branchHead(id),create:!ref});caches.set(id,{state:next,refSha:null,expiresAt:Date.now()+CACHE_TTL_MS});return clone(next);}catch(e){if(e.statusCode===409&&attempt<2)continue;throw e;}}throw Object.assign(new Error('Private Artifact Storen rinnakkaisuuskonflikti.'),{statusCode:409,code:'ARTIFACT_WRITE_CONFLICT'});}
export function __resetPrivateArtifactStoreForTests(){caches.clear();memoryStates.clear();}
